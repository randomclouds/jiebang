---
{"dg-publish":true,"permalink":"///"}
---

### 雷达图
```R
radarChart
    title 红队成员能力雷达图
    axis "漏洞利用", "隐蔽性", "横向移动", "持久化", "目标达成"
    "Alice" --> [85, 70, 90, 60, 95]
    "Bob" --> [60, 85, 75, 80, 70]
```

### 其他
```python
def extract_features(logs):
    return {
        "attack_speed": np.mean(logs["time_deltas"]),
        "tool_diversity": len(set(logs["tools"])),
        "stealth_score": 1 - logs["detection_rate"]
    }

from sklearn.cluster import KMeans
kmeans = KMeans(n_clusters=3).fit(features)
labels = ["先锋型", "隐匿型", "策略型"][kmeans.labels_]
```
